import React from "react";
import styled from "styled-components";
import tw from "twin.macro";

const Section = styled.div`
  ${tw`
height[500px]
bg-gray-200
`}
`;

const Fees = () => {
  return <Section> Fees </Section>;
};

export default Fees;
